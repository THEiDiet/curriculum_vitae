import React from 'react';
import c from "../styles/common.module.scss";
import s from "../styles/Portfolio.module.scss";

const Hiring = () => {
    return (
        <div>
            <h2 className={`${c.header}`}>Hire me</h2>
            <button>button</button>
        </div>
    );
};

export default Hiring;